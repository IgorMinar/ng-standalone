import { Component } from '@angular/core';

@Component({
  selector: 'standalone-component-demo',
  template: `
    <h1>First Standalone Component Demo</h1>
    <first-standalone-component></first-standalone-component>
  `
})
export class AppComponent {}
